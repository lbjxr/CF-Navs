# CF-Navs 更新日志

本文记录近期网页端、数据能力和浏览器扩展的功能调整。网页端与扩展保持解耦：普通页面更新不会要求用户重新安装或打包扩展。

## 近期更新

### 书签管理与排序

- 支持书签设置为“私密链接（仅登录可见）”。
- 支持分类设置为“访客不可见（仅登录可见）”，并自动隐藏其子分类和书签。
- 首页支持跨分类拖动排序；进入“排序”模式后，主分类和子分类都会展开为独立拖放区域，可以直接把书签从主分类拖到子分类，或在不同子分类之间移动。
- 排序操作先保存在前端草稿中，点击“保存排序”后一次性提交；点击“取消”不会修改服务器数据。
- 移动端增加触摸友好的编辑入口。平时不显示编辑按钮，只有进入排序/拖动模式后显示，避免遮挡书签卡片；电脑端右键编辑继续保留。

### 内网地址与浏览器同步

- 书签支持填写可选的内网地址；安装扩展后，访问书签时优先探测并打开可达的内网入口，失败则回退到外网地址。
- 浏览器书签同步采用“浏览器 → 导航页”单向模式，新书签统一进入“浏览器新增收藏”分类，不自动按浏览器文件夹创建导航分类，也不会删除现有导航页书签。
- 后台提供浏览器同步开关；关闭后服务端拒绝同步请求。
- 普通版扩展只提供书签同步；极客版提供书签同步和自定义新标签页功能。

### 网页与扩展解耦

- 网页端通过稳定的数据属性向扩展提供站点标识、内网地址和打开方式。
- 后续网页端的分类、编辑、页脚和样式更新，不需要重新打包扩展。
- 只有同步协议、新标签页、权限或扩展自身逻辑发生变化时，才需要重新生成扩展版本。

### 文档与署名

- 补充首页排序、私密书签、浏览器同步和扩展版本说明。
- 普通版和极客版扩展页面底部增加开发者署名及源码引用入口。

